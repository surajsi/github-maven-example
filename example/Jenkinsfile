pipeline {
		agent any
		
		stages {
			stage ('clean') {
			steps {
				tools {
  					maven 'maven'
							
				
				sh 'mvn clean'
				}
				}
				}
			stage ('package') {
			steps {
				
				sh 'mvn package'	
				
				}
				}
				}
				}
